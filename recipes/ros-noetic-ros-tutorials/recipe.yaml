package:
  name: ros-noetic-ros-tutorials
  version: 0.10.2
source:
  git_url: https://github.com/ros-gbp/ros_tutorials-release.git
  git_rev: release/noetic/ros_tutorials/0.10.2-1
  folder: ros-noetic-ros-tutorials/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 6
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
  host:
    - python
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-catkin
    - ros-noetic-roscpp-tutorials
    - ros-noetic-rospy-tutorials
    - ros-noetic-turtlesim
  run:
    - python
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-roscpp-tutorials
    - ros-noetic-rospy-tutorials
    - ros-noetic-turtlesim
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
