package:
  name: ros-noetic-trac-ik
  version: 1.6.6
source:
  git_url: https://github.com/traclabs/trac_ik-release.git
  git_rev: release/noetic/trac_ik/1.6.6-1
  folder: ros-noetic-trac-ik/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 12
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
  host:
    - python
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-catkin
  run:
    - python
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-trac-ik-examples
    - ros-noetic-trac-ik-kinematics-plugin
    - ros-noetic-trac-ik-lib
    - ros-noetic-trac-ik-python
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
