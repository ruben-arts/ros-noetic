package:
  name: ros-noetic-clear-costmap-recovery
  version: 1.17.1
source:
  git_url: https://github.com/ros-gbp/navigation-release.git
  git_rev: release/noetic/clear_costmap_recovery/1.17.1-1
  folder: ros-noetic-clear-costmap-recovery/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 10
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
  host:
    - eigen
    - python
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-catkin
    - ros-noetic-cmake-modules
    - ros-noetic-costmap-2d
    - ros-noetic-nav-core
    - ros-noetic-pluginlib
    - ros-noetic-roscpp
    - ros-noetic-rostest
    - ros-noetic-tf2-ros
  run:
    - eigen
    - python
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-costmap-2d
    - ros-noetic-nav-core
    - ros-noetic-pluginlib
    - ros-noetic-roscpp
    - ros-noetic-tf2-ros
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
