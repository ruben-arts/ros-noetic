package:
  name: ros-noetic-cv-camera
  version: 0.6.0
source:
  git_url: https://github.com/OTL/cv_camera-release.git
  git_rev: release/noetic/cv_camera/0.6.0-1
  folder: ros-noetic-cv-camera/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 10
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
    - sel(unix): "{{ cdt('mesa-libgl-devel') }}"
    - sel(unix): "{{ cdt('mesa-dri-drivers') }}"
    - sel(linux): "{{ cdt('libselinux') }}"
    - sel(linux): "{{ cdt('libxdamage') }}"
    - sel(linux): "{{ cdt('libxxf86vm') }}"
    - sel(linux): "{{ cdt('libxfixes') }}"
    - sel(linux): "{{ cdt('libxext') }}"
    - sel(linux): "{{ cdt('libxau') }}"
    - sel(linux): "{{ cdt('libxcb') }}"
  host:
    - libopencv
    - py-opencv
    - python
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-camera-info-manager
    - ros-noetic-catkin
    - ros-noetic-cv-bridge
    - ros-noetic-image-transport
    - ros-noetic-nodelet
    - ros-noetic-roscpp
    - ros-noetic-roslint
    - ros-noetic-rostest
    - ros-noetic-sensor-msgs
    - sel(unix): xorg-libx11
    - sel(unix): xorg-libxext
  run:
    - libopencv
    - py-opencv
    - python
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-camera-info-manager
    - ros-noetic-cv-bridge
    - ros-noetic-image-transport
    - ros-noetic-nodelet
    - ros-noetic-roscpp
    - ros-noetic-sensor-msgs
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
    - sel(unix): xorg-libx11
    - sel(unix): xorg-libxext
