package:
  name: ros-noetic-diagnostic-common-diagnostics
  version: 1.10.4
source:
  git_url: https://github.com/ros-gbp/diagnostics-release.git
  git_rev: release/noetic/diagnostic_common_diagnostics/1.10.4-1
  folder: ros-noetic-diagnostic-common-diagnostics/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 6
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
  host:
    - psutil
    - python
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-catkin
    - ros-noetic-diagnostic-updater
    - ros-noetic-rospy
    - ros-noetic-rostest
    - ros-noetic-tf
  run:
    - psutil
    - python
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-diagnostic-updater
    - ros-noetic-rospy
    - ros-noetic-tf
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
