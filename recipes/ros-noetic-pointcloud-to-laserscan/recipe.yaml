package:
  name: ros-noetic-pointcloud-to-laserscan
  version: 1.4.1
source:
  git_url: https://github.com/ros-gbp/pointcloud_to_laserscan-release.git
  git_rev: release/noetic/pointcloud_to_laserscan/1.4.1-1
  folder: ros-noetic-pointcloud-to-laserscan/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 10
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
  host:
    - python
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-catkin
    - ros-noetic-laser-geometry
    - ros-noetic-message-filters
    - ros-noetic-nodelet
    - ros-noetic-roscpp
    - ros-noetic-roslint
    - ros-noetic-sensor-msgs
    - ros-noetic-tf2
    - ros-noetic-tf2-ros
    - ros-noetic-tf2-sensor-msgs
  run:
    - python
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-laser-geometry
    - ros-noetic-message-filters
    - ros-noetic-nodelet
    - ros-noetic-roscpp
    - ros-noetic-sensor-msgs
    - ros-noetic-tf2
    - ros-noetic-tf2-ros
    - ros-noetic-tf2-sensor-msgs
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
