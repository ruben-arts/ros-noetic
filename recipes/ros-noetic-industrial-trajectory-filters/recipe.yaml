package:
  name: ros-noetic-industrial-trajectory-filters
  version: 0.7.2
source:
  git_url: https://github.com/ros-industrial-release/industrial_core-release.git
  git_rev: release/noetic/industrial_trajectory_filters/0.7.2-1
  folder: ros-noetic-industrial-trajectory-filters/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 12
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
  host:
    - orocos-kdl
    - python
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-catkin
    - ros-noetic-class-loader
    - ros-noetic-moveit-core
    - ros-noetic-moveit-ros-planning
    - ros-noetic-pluginlib
    - ros-noetic-trajectory-msgs
  run:
    - orocos-kdl
    - python
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-class-loader
    - ros-noetic-moveit-core
    - ros-noetic-moveit-ros-planning
    - ros-noetic-pluginlib
    - ros-noetic-trajectory-msgs
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
