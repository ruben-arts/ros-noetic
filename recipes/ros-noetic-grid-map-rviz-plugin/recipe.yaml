package:
  name: ros-noetic-grid-map-rviz-plugin
  version: 1.6.4
source:
  git_url: https://github.com/anybotics/grid_map-release.git
  git_rev: release/noetic/grid_map_rviz_plugin/1.6.4-1
  folder: ros-noetic-grid-map-rviz-plugin/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 10
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
  host:
    - gtest
    - python
    - qt 5
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-catkin
    - ros-noetic-grid-map-msgs
    - ros-noetic-grid-map-ros
    - ros-noetic-rviz
  run:
    - python
    - qt
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-grid-map-msgs
    - ros-noetic-grid-map-ros
    - ros-noetic-rviz
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
