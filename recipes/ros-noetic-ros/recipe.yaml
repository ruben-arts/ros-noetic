package:
  name: ros-noetic-ros
  version: 1.15.8
source:
  git_url: https://github.com/ros-gbp/ros-release.git
  git_rev: release/noetic/ros/1.15.8-1
  folder: ros-noetic-ros/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 6
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
  host:
    - python
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-catkin
    - ros-noetic-mk
    - ros-noetic-rosbash
    - ros-noetic-rosboost-cfg
    - ros-noetic-rosbuild
    - ros-noetic-rosclean
    - ros-noetic-roscreate
    - ros-noetic-roslang
    - ros-noetic-roslib
    - ros-noetic-rosmake
    - ros-noetic-rosunit
  run:
    - python
    - ros-distro-mutex 0.2 noetic
    - ros-noetic-catkin
    - ros-noetic-mk
    - ros-noetic-rosbash
    - ros-noetic-rosboost-cfg
    - ros-noetic-rosbuild
    - ros-noetic-rosclean
    - ros-noetic-roscreate
    - ros-noetic-roslang
    - ros-noetic-roslib
    - ros-noetic-rosmake
    - ros-noetic-rosunit
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
